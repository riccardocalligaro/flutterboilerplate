/// Class that contains all the app routes
class Routes {
  Routes._();

  static const String splash = '/splash';
  static const String home = '/home';

  static var routes = {
    // splash: (context) => SlashScreen()
  };
}
